# Résumé / CV

Expected file: `bassel-azab-resume.pdf`

Drop your real résumé PDF here. `CvDownloadCard` (in
`src/components/CvDownloadCard.tsx`) checks whether this file exists on
load and shows a working "Download CV" button if so, or an honest "CV
coming soon" state if not — no build step needed, just add the file and
reload.
